legacy-peer-deps=false

# NOTE for Hostinger: do NOT set omit=dev=false in this file or in environment
# variables — that is invalid npm syntax and will warn. Build tools (vite, etc.)
# are listed in "dependencies" so they install even with NODE_ENV=production.
