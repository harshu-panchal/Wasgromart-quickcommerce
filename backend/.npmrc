legacy-peer-deps=false

# Build uses esbuild (in dependencies). Do NOT set omit=dev=false here or in
# Hostinger env vars — that is invalid npm syntax.
#
# Local dev: if `npm run dev` fails with missing tsx/preflight.cjs, run:
#   npm run dev:setup
# (NODE_ENV=production skips devDependencies like tsx and typescript.)
